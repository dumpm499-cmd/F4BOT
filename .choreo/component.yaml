schemaVersion: "1.0"
buildPreset: docker
dockerfilePath: Dockerfile
buildContext: .
endpoints:
  - name: bot-link
    service:
      basePath: /
      port: 8000
    type: REST
    networkVisibilities:
      - Public
